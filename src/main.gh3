fn add(a, b) {
    a + b
}

let obj = {
    x: 100,
    y: 30,
    aggiungi: add,
    test: {
        ciao: {
            cacca: 10,
            culo: true
        }
    }
}

// commento singolo
let j = obj.x + obj.y
/* commento
   multilinea */

print(j)
print(obj)
